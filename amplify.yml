version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - npm start
    build:
      commands:
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths:
